import { MotionDirective } from './motion.directive';

describe('MotionDirective', () => {
  it('should create an instance', () => {
    const directive = new MotionDirective();
    expect(directive).toBeTruthy();
  });
});
